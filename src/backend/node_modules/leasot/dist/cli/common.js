"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.outputTodos = void 0;
const __1 = require("..");
const outputTodos = (todos, options) => {
    try {
        const output = __1.report(todos, options.reporter);
        console.log(output);
    }
    catch (e) {
        console.error(e);
    }
    if (options.exitNicely) {
        process.exit(0);
    }
    process.exit(todos.length ? 1 : 0);
};
exports.outputTodos = outputTodos;
